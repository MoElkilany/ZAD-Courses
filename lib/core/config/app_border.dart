import 'package:flutter/material.dart';

/// Border Radius for circular shape with radius 4
const BorderRadius appBordRadCir4 = BorderRadius.all(Radius.circular(4));

/// Border Radius for circular shape with radius 8
const BorderRadius appBordRadCir8 = BorderRadius.all(Radius.circular(8));

/// Border Radius for circular shape with radius 16
const BorderRadius appBordRadCir16 = BorderRadius.all(Radius.circular(16));

/// Border Radius for circular shape with radius 24
const BorderRadius appBordRadCir24 = BorderRadius.all(Radius.circular(24));

/// Border Radius for circular shape with radius 50
const BorderRadius appBordRadCir50 = BorderRadius.all(Radius.circular(50));

/// Border Radius for horizontal shape with radius 4
const BorderRadius appBordRadH4 = BorderRadius.horizontal(
  left: Radius.circular(4),
  right: Radius.circular(4),
);

/// Border Radius for horizontal shape with radius 8
const BorderRadius appBordRadH8 = BorderRadius.horizontal(
  left: Radius.circular(8),
  right: Radius.circular(8),
);

/// Border Radius for horizontal shape with radius 16
const BorderRadius appBordRadH16 = BorderRadius.horizontal(
  left: Radius.circular(16),
  right: Radius.circular(16),
);

/// Border Radius for horizontal shape with left radius 4
const BorderRadius appBordRadHL4 = BorderRadius.horizontal(
  left: Radius.circular(4),
);

/// Border Radius for horizontal shape with left radius 8
const BorderRadius appBordRadHL8 = BorderRadius.horizontal(
  left: Radius.circular(8),
);

/// Border Radius for horizontal shape with left radius 16
const BorderRadius appBordRadHL16 = BorderRadius.horizontal(
  left: Radius.circular(16),
);

/// Border Radius for horizontal shape with right radius 4
const BorderRadius appBordRadHR4 = BorderRadius.horizontal(
  right: Radius.circular(4),
);

/// Border Radius for horizontal shape with right radius 8
const BorderRadius appBordRadHR8 = BorderRadius.horizontal(
  right: Radius.circular(8),
);

/// Border Radius for horizontal shape with right radius 16
const BorderRadius appBordRadHR16 = BorderRadius.horizontal(
  right: Radius.circular(16),
);

/// Border Radius for top left and top right only with radius 4
const BorderRadius appBordRadTLR4 = BorderRadius.only(
  topLeft: Radius.circular(4),
  topRight: Radius.circular(4),
);

/// Border Radius for top left and top right only with radius 8
const BorderRadius appBordRadTLR8 = BorderRadius.only(
  topLeft: Radius.circular(8),
  topRight: Radius.circular(8),
);

/// Border Radius for top left and top right only with radius 16
const BorderRadius appBordRadTLR16 = BorderRadius.only(
  topLeft: Radius.circular(16),
  topRight: Radius.circular(16),
);

/// Border Radius for bottom left and bottom right only with radius 4
const BorderRadius appBordRadBLR4 = BorderRadius.only(
  bottomLeft: Radius.circular(4),
  bottomRight: Radius.circular(4),
);

/// Border Radius for bottom left and bottom right only with radius 8
const BorderRadius appBordRadBLR8 = BorderRadius.only(
  bottomLeft: Radius.circular(8),
  bottomRight: Radius.circular(8),
);

/// Border Radius for bottom left and bottom right only with radius 16
const BorderRadius appBordRadBLR16 = BorderRadius.only(
  bottomLeft: Radius.circular(16),
  bottomRight: Radius.circular(16),
);
